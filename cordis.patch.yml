# dsh-conversation-rail bundle 层：把会话小地图插进 profile 的插件表。
#
# host 侧只开一个只读的大纲接口（见 src/index.ts），界面全在 dsh.client
# 声明的浏览器入口里（对话左缘的导航轨 + 悬停预览卡）。
- insert:
    - id: dsh-conversation-rail
      name: 'dsh-conversation-rail'
      config: {}
